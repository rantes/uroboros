<!DOCTYPE html>
<html lang="en">
<head>
    <? require_once(INST_PATH.'app/views/_header-contents.phtml'); ?>
</head>
<body>
    <noscript>
        <h1>Your browser does not support JS</h1>
    </noscript>
    <dmb-view id="page" class="<?= ($this->operationalShell ?? false) ? 'operational' : ''; ?>">
	    <dmb-header class="section group">
            <header>
                <div class="col col2"></div>
                <div class="col col8">Uroboros CI/CD</div>
                <div class="col col2"><a href="/index/logout">Cerrar sesi&oacute;n</a></div>
                <? if ($this->operationalShell ?? false): ?>
                    <dmb-operational-topbar data-user-initials="<?= currentUserInitials(); ?>"></dmb-operational-topbar>
                <? endif; ?>
            </header>
        </dmb-header>
        <dmb-content>
        <?=$this->yield;?>
        </dmb-content>
        <dmb-footer class="section group">
            <? if ($this->operationalShell ?? false): ?>
            <? $__oemFooterMetrics = footerMetrics(); ?>
            <dmb-oem-metrics-footer
                data-events="<?= $__oemFooterMetrics['events']; ?>"
                data-commands-dispatched="<?= $__oemFooterMetrics['command_dispatched']; ?>"
                data-commands-failed="<?= $__oemFooterMetrics['command_failed']; ?>"
                data-reactions-executed="<?= $__oemFooterMetrics['reaction_executed']; ?>"
                data-reactions-failed="<?= $__oemFooterMetrics['reaction_failed']; ?>"
                data-oem-status="<?= oemStatus(); ?>">
            </dmb-oem-metrics-footer>
        <? endif; ?>
            <div class="col col12">
                <span>Uroboros CI/CD</span>
                <span class="footer-version">v1.0.0</span>
            </div>
        </dmb-footer>
    <? if ($this->operationalShell ?? false): ?>
        <? require_once(INST_PATH.'app/views/_sidebar-operational.phtml'); ?>
        <dmb-panel id="panel-form-add-edit-reg" class="dmb-panel right small"></dmb-panel>
    <? endif; ?>
    </dmb-view>
    <? require_once(INST_PATH.'app/views/_footer-contents.phtml'); ?>
</body>
</html>