<dmb-table>
    <table>
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            <? foreach ($this->data as $row): ?>
            <tr>
                <td><?= $row->name; ?></td>
                <td>
                    <dmb-more-options>
                        <dmb-more-option
                            behavior="open-panel"
                            url="/admin/groups/edit/<?= $row->id; ?>"
                            panel="#panel-form-add-edit-reg">
                            Editar
                        </dmb-more-option>
                        <dmb-more-option
                            behavior="ajax"
                            url="/admin/groups/<?= $row->id; ?>">
                            Eliminar
                        </dmb-more-option>
                    </dmb-more-options>
                </td>
            </tr>
            <? endforeach; ?>
        </tbody>
    </table>
</dmb-table>

<dmb-dock id="admin-dock-options">
    <div class="item" data-title="Agregar">
        <dmb-button-action
            action="new"
            panel="#panel-form-add-edit-reg"
            url="/admin/groups/add"
            behavior="open-panel">
        </dmb-button-action>
    </div>
</dmb-dock>

<dmb-pagination>
    Página: <?= $this->data->WillPaginate($this->fullUrl()); ?>
    de <?= $this->data->PaginateTotalPages; ?>
</dmb-pagination>
