<dmb-table>
    <table>
        <thead>
            <tr>
                <th>Nombre</th>
                <th>Creado</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            <? foreach ($this->data as $row): ?>
            <tr>
                <td><?= htmlspecialchars((string) $row->name, ENT_QUOTES, 'UTF-8'); ?></td>
                <td><?= date('Y-m-d H:i:s', $row->created_at); ?></td>
                <td>
                    <dmb-more-options>
                        <dmb-more-option
                            behavior="open-panel"
                            url="/admin/project_credentials/edit/<?= $row->id; ?>?project_id=<?= $this->projectId; ?>"
                            panel="#panel-form-add-edit-reg">
                            Cambiar valor
                        </dmb-more-option>
                        <dmb-more-option
                            behavior="ajax"
                            url="/admin/project_credentials/<?= $row->id; ?>">
                            Eliminar
                        </dmb-more-option>
                    </dmb-more-options>
                </td>
            </tr>
            <? endforeach; ?>
        </tbody>
    </table>
</dmb-table>

<dmb-button-action
    action="new"
    panel="#panel-form-add-edit-reg"
    url="/admin/project_credentials/add?project_id=<?= $this->projectId; ?>"
    behavior="open-panel">
    Agregar credencial
</dmb-button-action>

<dmb-pagination>
    Página: <?= $this->data->WillPaginate($this->fullUrl()); ?>
    de <?= $this->data->PaginateTotalPages; ?>
</dmb-pagination>
