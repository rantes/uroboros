<dmb-table>
    <table>
        <thead>
            <tr>
                <th>Orden</th>
                <th>Nombre</th>
                <th>Tipo</th>
                <th>Comando</th>
                <th>Acciones</th>
            </tr>
        </thead>
        <tbody>
            <? foreach ($this->data as $row): ?>
            <tr>
                <td><?= $row->step_order; ?></td>
                <td><?= $row->name; ?></td>
                <td><?= $row->type; ?></td>
                <td><code><?= htmlspecialchars((string) $row->command, ENT_QUOTES, 'UTF-8'); ?></code></td>
                <td>
                    <dmb-more-options>
                        <dmb-more-option
                            behavior="open-panel"
                            url="/admin/workflow_step_definitions/edit/<?= $row->id; ?>?workflow_definition_id=<?= $this->workflowDefinitionId; ?>"
                            panel="#panel-form-add-edit-reg">
                            Editar
                        </dmb-more-option>
                        <dmb-more-option
                            behavior="ajax"
                            url="/admin/workflow_step_definitions/<?= $row->id; ?>">
                            Eliminar
                        </dmb-more-option>
                    </dmb-more-options>
                </td>
            </tr>
            <? endforeach; ?>
        </tbody>
    </table>
</dmb-table>

<dmb-button-action
    action="new"
    panel="#panel-form-add-edit-reg"
    url="/admin/workflow_step_definitions/add?workflow_definition_id=<?= $this->workflowDefinitionId; ?>"
    behavior="open-panel">
    Agregar paso
</dmb-button-action>

<dmb-pagination>
    Página: <?= $this->data->WillPaginate($this->fullUrl()); ?>
    de <?= $this->data->PaginateTotalPages; ?>
</dmb-pagination>
