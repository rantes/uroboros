<dmb-simple-form <?= !empty($this->data->id) ? 'update' : ''; ?> redirect="/admin/workflow_step_definitions?workflow_definition_id=<?= $this->data->workflow_definition_id ?: $this->workflowDefinitionId; ?>" close-panel="#panel-form-add-edit-reg">
    <dmb-form
        id="form-workflow-step-definition"
        name="workflow_step_definition"
        action="/admin/workflow_step_definitions"
        method="post"
        async>
        <header>
            <dmb-close-panel orientation="right"></dmb-close-panel>
            <h3><?= $this->title; ?></h3>
        </header>
        <dmb-content>
            <div class="section group">
                <? if (!empty($this->data->id)): ?>
                <dmb-input type="hidden" dmb-name="workflow_step_definition[id]" dmb-value="<?= $this->data->id; ?>"></dmb-input>
                <? endif; ?>
                <dmb-input
                    type="hidden"
                    dmb-name="workflow_step_definition[workflow_definition_id]"
                    dmb-value="<?= $this->data->workflow_definition_id ?: $this->workflowDefinitionId; ?>">
                </dmb-input>
                <div class="col col12">
                    <dmb-input
                        label="Nombre"
                        dmb-name="workflow_step_definition[name]"
                        dmb-value="<?= $this->data->name; ?>"
                        validate="required">
                    </dmb-input>
                </div>
                <div class="col col6 col6-md col6-sm">
                    <dmb-select label="Tipo" dmb-name="workflow_step_definition[type]" dmb-value="<?= $this->data->type; ?>" validate="required">
                        <option value="">Seleccione...</option>
                        <option value="build">Build</option>
                        <option value="deploy">Deploy</option>
                        <option value="rollback">Rollback</option>
                        <option value="migration">Migration</option>
                        <option value="verification">Verification</option>
                        <option value="custom">Custom</option>
                    </dmb-select>
                </div>
                <div class="col col6 col6-md col6-sm">
                    <dmb-input
                        label="Orden"
                        dmb-name="workflow_step_definition[step_order]"
                        dmb-value="<?= $this->data->step_order; ?>"
                        type="number"
                        validate="required,numeric">
                    </dmb-input>
                </div>
                <div class="col col12">
                    <dmb-input
                        label="Comando"
                        dmb-name="workflow_step_definition[command]"
                        dmb-value="<?= htmlspecialchars((string) $this->data->command, ENT_QUOTES, 'UTF-8'); ?>"
                        validate="required">
                    </dmb-input>
                </div>
            </div>
        </dmb-content>
        <dmb-footer class="section group">
            <div class="col col6">
                <dmb-button type="reset">Cancelar</dmb-button>
            </div>
            <div class="col col6">
                <dmb-button type="submit">Guardar</dmb-button>
            </div>
        </dmb-footer>
    </dmb-form>
</dmb-simple-form>
